{
  "name": "artillery-loadtest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "artillery run -e test --insecure --output report.json config.yml",
    "test:dev": "artillery run -e dev -s --insecure config.yml",
    "test:debug": "DEBUG=http artillery run -e dev -s --insecure config.yml",
    "test:debug:all": "DEBUG=http* artillery run -e dev --insecure config.yml"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "artillery": "^2.0.0-35",
    "jsonwebtoken": "^9.0.0",
    "pg-promise": "^10.12.1"
  }
}
